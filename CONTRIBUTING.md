# Contributing

Use Python 3.10+. Run `python -m unittest discover -v` and `python examples/quickstart.py`.
Keep runtime dependencies standard-library-only. Add a failing synthetic regression test for each fix.
Document changes to capture formats, scope, ledger migration or READY semantics.
Never submit private evidence or employer-owned material without permission. See SECURITY.md.
